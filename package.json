{
  "name": "liremonde",
  "version": "1.0.0",
  "description": "Bibliothèque interactive LireMonde",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.1"
  }
}